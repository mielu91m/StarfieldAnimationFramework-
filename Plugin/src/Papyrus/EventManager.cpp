#include "PCH.h"
#include "Papyrus/EventManager.h"

// EventManager implementations are inline in EventManager.h (Starfield API:
// RegisterScript/UnregisterScript, Papyrus::VMHandle; no RE::VMHandle or IFunctionArguments).
// This file exists for the build and possible future non-inline additions.

namespace Papyrus
{
	// Reserved for future EventManager helpers if needed.
}
