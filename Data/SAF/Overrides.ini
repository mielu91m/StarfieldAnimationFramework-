; SAF animation overrides (key = animation path).
; Example usage in game: "saf play R1"
;
; Use absolute or relative paths. Relative paths are resolved from Data\.

R1=R_ArmUp
R2=R_Wave
R3=R_Point
